import { AgentExecuteOptions, AgentResult } from "@/types/agent";
import { LogLine } from "@/types/log";
import { ToolSet } from "ai/dist";
import { LLMClient } from "../llm/LLMClient";
import { StagehandPage } from "../StagehandPage";
export declare class StagehandOperatorHandler {
    private stagehandPage;
    private logger;
    private llmClient;
    private messages;
    private allTools;
    private logInferenceToFile;
    constructor(stagehandPage: StagehandPage, logger: (message: LogLine) => void, llmClient: LLMClient, mcpTools: ToolSet, logInferenceToFile?: boolean);
    private getTemperatureForModel;
    execute(instructionOrOptions: string | AgentExecuteOptions): Promise<AgentResult>;
    private getNextStep;
    private getSummary;
}
